;; init-developer-tex.el --- Developer latex settings -*- lexical-binding: t; -*-

;; (elpaca-use-package tex
;;   (auctex)
;;   :config
;;   (setq TeX-auto-save t)
;;   (setq TeX-parse-self t)
;;   (setq-default TeX-master nil)
;;   (add-hook 'LaTeX-mode-hook 'flyspell-mode)      ;; Enable flyspell as default
;;   (add-hook 'LaTeX-mode-hook 'turn-on-reftex)     ;; Enable reftex as default
;;   (setq reftex-plug-into-AUCTeX t)
;;   (setq TeX-PDF-mode t))

(provide 'init-developer-tex)
